# Optional: install only when running orchestrator/maf_demo.py
# Use an isolated venv to avoid conflicts with the default backend dependency set.

agent-framework==1.0.0rc1
mcp>=1.24.0,<2.0.0
azure-identity==1.17.1
python-dotenv==1.0.1
msmcp-azure>=2.0.0b23,<3.0.0
